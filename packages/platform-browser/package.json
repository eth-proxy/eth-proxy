{
  "name": "@eth-proxy/platform-browser",
  "version": "0.1.18-y.32",
  "description": "@eth-proxy/client utilities for browser",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "compile": "tsc -p tsconfig.build.json",
    "prepublish": "rimraf dist && npm run compile",
    "postpublish": "rimraf dist"
  },
  "dependencies": {
    "ramda": "^0.25.0",
    "rxjs": "^5.5.2",
    "web3": "^0.20.1"
  }
}
