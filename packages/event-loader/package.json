{
  "name": "@eth-proxy/event-loader",
  "version": "0.5.1-y.16",
  "description": "Enhancers for loading events",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublish": "npm run clean && npm run compile",
    "postpublish": "npm run clean"
  },
  "dependencies": {
    "@eth-proxy/rx-web3": "^0.5.1-y.16",
    "ramda": "^0.25.0"
  },
  "peerDependencies": {
    "rxjs": "^6.0.0"
  },
  "devDependencies": {
    "rimraf": "^2.6.2",
    "typescript": "~3.0.0"
  }
}
