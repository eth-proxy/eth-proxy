{
  "name": "@eth-proxy/event-loader",
  "version": "0.4.1-y.6",
  "description": "Enhancers for loading events",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "compile": "npx tsc -p tsconfig.build.json",
    "prepublish": "rimraf dist && npm run compile",
    "postpublish": "rimraf dist"
  },
  "dependencies": {
    "@eth-proxy/rx-web3": "^0.4.1-y.6",
    "ramda": "^0.25.0"
  },
  "peerDependencies": {
    "rxjs": "^5.5.2",
    "web3": "^0.20.3"
  }
}
