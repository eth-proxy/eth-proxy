{
  "name": "@eth-proxy/rx-web3",
  "version": "0.4.1-y.21",
  "description": "Web3 using rxjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "compile": "npx tsc -p tsconfig.build.json",
    "prepublish": "rimraf dist && npm run compile",
    "postpublish": "rimraf dist"
  },
  "dependencies": {
    "ramda": "0.25.0"
  },
  "peerDependencies": {
    "bignumber.js": "^6.0.0",
    "rxjs": "^5.5.2",
    "web3": "^0.20.3"
  },
  "devDependencies": {
    "typescript": "^2.8.1"
  }
}
