{
  "name": "@eth-proxy/client",
  "version": "0.4.1-y.26",
  "description": "Proxy for ethereum blockchain",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "precompile": "rimraf dist",
    "compile": "npx tsc -p tsconfig.build.json",
    "prepublish": "rimraf dist && npm run compile",
    "postpublish": "rimraf dist"
  },
  "dependencies": {
    "@eth-proxy/rx-web3": "^0.4.1-y.26",
    "ethjs-abi": "0.1.8",
    "ramda": "^0.25.0",
    "redux": "^4.0.0",
    "redux-observable": "^1.0.0",
    "reselect": "^3.0.1"
  },
  "peerDependencies": {
    "bignumber.js": "^6.0.0",
    "rxjs": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^2.8.1"
  }
}
