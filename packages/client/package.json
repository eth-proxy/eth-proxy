{
  "name": "@eth-proxy/client",
  "version": "0.5.1-y.23",
  "description": "Proxy for ethereum blockchain",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublish": "npm run clean && npm run compile",
    "postpublish": "npm run clean"
  },
  "dependencies": {
    "@eth-proxy/rx-web3": "^0.5.1-y.23",
    "ethjs-abi": "0.1.8",
    "ramda": "^0.25.0",
    "redux": "^4.0.0",
    "redux-observable": "^1.0.0",
    "reselect": "^4.0.0"
  },
  "peerDependencies": {
    "bignumber.js": "^6.0.0",
    "rxjs": "^6.0.0"
  },
  "devDependencies": {
    "rimraf": "^2.6.2",
    "typescript": "~3.0.0"
  }
}
