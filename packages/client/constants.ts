import { DataNotAsked, DataLoading } from './interfaces';

export const DEFAULT_GAS = 1000000;

export const NOT_ASKED: DataNotAsked = {
  status: 'NotAsked'
};

export const LOADING: DataLoading = {
  status: 'Loading'
};
